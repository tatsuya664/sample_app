<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/micropost', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items, renderer: WillPaginate::ActionView::BootstrapLinkRenderer%>
<% end %>