<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
              html: { method: :delete },
              data: { turbo_frame: "follow_form_#{@user.id}" }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-secondary" %>
<% end %>