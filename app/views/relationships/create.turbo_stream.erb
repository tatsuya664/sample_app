<%= turbo_stream.replace "follow_form_#{@user.id}" do %>
  <%= render "relationships/unfollow", user: @user %>
<% end %>
<%= turbo_stream.replace "stats_#{@user.id}" do %>
  <%= render "shared/stats", user: @user %>
<% end %>